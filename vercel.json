{
  "framework": null,
  "buildCommand": "cd backend && npm install && npx prisma generate && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "devCommand": "cd backend && npm run start:dev",
  "regions": ["sin1"],
  "env": {
    "NODE_ENV": "production"
  }
}
